---
name: eos-log-to-rootcause
description: 从报错日志堆栈反查 Java 分层应用（Spring Boot 分层 fatjar / war / 带 patch 目录的任何产品）的真实根因——自动发现介质布局、判定类归属与覆盖关系、用前导零或 ASCII 加载序仲裁版本、反编译目标类、以 LineNumberTable 对齐行号、定位到具体源码语句。当用户粘贴 Java 报错日志并要求分析根因、需要判断某个类来自主归档还是 patch/覆盖 jar、或需要反编译安装目录下的 class 时使用本技能。产品无关，不绑定任何厂商。
---

# Java 报错日志 → 根因定位

> 输入是**用户粘贴的报错日志**（或截图），不是介质内的 `logs/` 目录。
> 输出是**定位到具体源码语句的根因**，并区分「代码缺陷 / 环境配置 / 版本错配 / 数据问题」。

**本技能与产品无关**：不认识任何厂商名、包名或目录约定。
介质布局、覆盖目录、应用命名空间都在运行时**自动发现**（`media.py`）；
工具（JDK / 反编译器）在运行时**自动定位**（`env.py`）。
已实测适用于两种**结构完全不同**的介质（见「通用性验证」）。

## 适用与不适用

**适用**：任何 Java 分层打包的应用——
Spring Boot 分层 fatjar（`BOOT-INF/classes` + `BOOT-INF/lib`）、war（`WEB-INF/…`）、ear、
以及**在主归档之外还有覆盖/patch 目录**（`lib/`、`patch/`、`plugins/`、`-Dloader.path=…`）的部署形态。
产品类未混淆即可。

**不适用**：类名被混淆（`a/b/c.class`）时行号法失效，只能做行为分析；
日志无堆栈（纯业务错误码）时走配置/数据排查；
类从安装目录之外加载（共享 lib、容器镜像、远端 classloader）时，本技能只能给出"不在介质内"的结论。

---

## 核心链路

```
⓪ 发现布局  →  ① 提取堆栈帧  →  ② 判类归属  →  ③ 覆盖仲裁  →  ④ 反编译  →  ⑤ 行号对齐  →  ⑥ 根因定性
```

**最关键的是 ⑤**：日志行号**不等于**反编译输出的行号，也不等于"方法区间内的某一行"。
必须逐行走 `LineNumberTable` 把源码行锚定到字节码偏移。**跳过这步必然读错代码**（本项目实测犯过这个错）。

**第二关键的是 ③**：只要存在覆盖目录，同名类就有多份，"哪一份生效"必须用**证据**判定，
不能按日期、不能按版本号、也不能按"新的优先"。

---

## ⓪ 第一步：发现介质布局（不要假设任何目录名）

```powershell
$PY = 'C:\Program Files\Python312\python.exe'          # 任意 3.8+ 即可
$SK = "$env:USERPROFILE\.dsh\skills\eos-log-to-rootcause\scripts"
$MEDIA = '<产品的安装目录>'

# 一条命令看清：主归档是哪个、哪些目录会覆盖它、应用自己的包命名空间是什么
& $PY -X utf8 "$SK\find_class.py" --media $MEDIA --layout
```

输出示例（两种真实介质，结构完全不同，均为自动识别）：

| 介质 | 主归档 | 覆盖区 | 被覆盖的类 | 自动学出的命名空间 |
|---|---|---|---|---|
| A | `*-boot-...-exec.jar`（boot-layered，379 MB，827 嵌套 jar，`BOOT-INF/classes` 仅 **1** 个类） | `lib/`（25 jar） | **370** | `com.primeton` |
| B | `*-exec.jar`（boot-layered，283 MB，373 嵌套 jar，`BOOT-INF/classes` **414** 个类） | `lib/`（1 jar） | **88** | `com.primeton` |

**自动发现做了这些事**（全部来自实测事实，无产品假设）：

- 识别打包形态：`boot-layered`（`BOOT-INF/classes`+`BOOT-INF/lib`）、`war`、`ear`、`plain`；
- 主归档 = **分层归档优先**（否则会误选依赖 jar：实测 `redisson-*.jar` 顶层 1253 个类，
  比 fatjar 自带的 1 个 `BOOT-INF/classes` 类还多，按类数排序必然选错）；
- 覆盖目录 = 目录名像 `lib/patch/hotfix/sp/plugins/…`，**或**其中 jar 与主归档存在**同名类交集**；
- 应用命名空间 = 从主归档自带类 + 被覆盖的类里**推断**，不预设。

> ⚠️ **别跳过这一步去猜目录**。同类产品的不同版本打包风格可能完全不同：
> 介质 A 的应用类几乎全在**嵌套 jar** 里，介质 B 的应用类几乎全在 **`BOOT-INF/classes/`**。
> 只扫其中一个位置，就会对另一种介质报出"类不存在"的**假结论**（实测踩过）。

---

## ⚡ 首要取巧点：**先查覆盖目录**，主归档只做兜底

**结论（已实测）**：产品**核心业务代码在覆盖目录（patch jar）里就是完整类体**，绝大多数根因分析**不需要打开主归档**。

原理：patch 是**整类替换（whole-class override）**，不是差量补丁。patch jar 里的类就是该类的完整字节码，
所以「反编译目标类」直接对 patch jar 做即可——**不需要**去主归档里找同名原版。

### 实测代价（介质 A）

| 路径 | 规模 | 耗时 |
|---|---|---|
| 覆盖目录 `lib/`（29 jar） | 4688 class | **0.19 s** 建索引 / **0.03 s** 定位单类 |
| 主归档（379 MB） | 197177 class（827 嵌套 jar） | 3.03 s（定向扫 0.21 s） |

覆盖目录里有 **142 个产品类**：**112 个**主归档也有同名原版（patch 覆盖原版），
**30 个**是 patch 新引入的（主归档里根本没有）→ **只查主归档会直接漏掉这些类**。
反过来主归档内有 **12150 个产品类**从未被 patch，只能从主归档取。
所以是**顺序**问题：

```
先覆盖目录（0.19 s，覆盖全部被 patch 过的核心业务类）
   └─ 命中 → 直接仲裁 + 反编译，结束，不碰主归档
   └─ 未命中 → 该类未被 patch，才走主归档兜底
```

### 一条命令走完全流程

```powershell
# 定位 + 列出所有副本 + 用日志行号仲裁生效版本（默认纯 Python 后端，不起 JVM）
& $PY -X utf8 "$SK\find_class.py" <全限定类名> --media $MEDIA --method <方法> --line <日志行号>
```

---

## ⚡ 性能：**不要用 javap 做批量解析**（提速 178 倍）

`javap` 每次调用都要**启动一个 JVM**，实测 **0.36 s/次**。而仲裁需要**每一个不同类体**的行号表，一个类有 4~5 个候选就要几秒——**瓶颈完全在这里**，不在磁盘或 CPU。

**行号表本来就写死在 class 文件里**，直接用 Python 读字节码即可，无需起进程：

| 后端 | 单类耗时 | 全介质 413 个产品类 | 实现 |
|---|---|---|---|
| `javap -p -l` | **0.36 s** | 约 **219 s** | 起 JVM + 解析文本 |
| **纯 Python**（`classfile.py`） | **0.002 s** | **0.44 s** | 直接读常量池 → 方法表 → `Code` → `LineNumberTable` |

**实测提速 178 倍**（同一类：0.0020 s vs 0.3595 s，`javap` 冷盘另有 0.36–0.9 s 的波动）。端到端脚本层面：仲裁场景 **0.10 s vs 0.85 s（约 8 倍）**——剩下的时间是扫目录与 CRC 分组。

**默认就是纯 Python 后端**，`find_class.py` 支持 `--backend py|javap`：

```powershell
# 默认快速后端（不需要 JDK）
& $PY -X utf8 "$SK\find_class.py" <FQCN> --media $MEDIA --method <m> --line <N>

# 需要交叉验证时，用 javap 后端跑同一个类，输出应当逐字相同
& $PY -X utf8 "$SK\find_class.py" <FQCN> --media $MEDIA --method <m> --line <N> --backend javap
```

### 正确性保证（不是猜测）

纯 Python 后端与 javap 做了**逐行全等校验**，两重验证：

1. **全介质等价**：142 个产品类里 134 个有行号表的，纯 Python 解析结果与 javap **逐行完全一致，0 处不符、0 异常**（无行号表的 8 个两边同为空）。
2. **输出级等价**：7 个真实分析场景（构造帧、空洞行、合成方法、`--cross` 跨版本、命中/未命中）分别用两个后端跑，**输出逐字相同**（仅耗时数字不同）。

> 校验套件：`scripts/tests/run_tests.py`（三套一起跑）。**改动任一脚本后必跑**。

`javap` **仍然保留**且不可替代的用途：**要看字节码指令文本**（`-c`）时，以及作为**异常情况的对照基准**。行号表本身用纯 Python 即可。

---

## 环境与工具（全部运行时自动定位）

**没有任何路径写死在代码里**。`scripts/env.py` 按 `环境变量 → PATH → 常见安装位置` 三级查找：
| 工具 | 环境变量 | 用途 | 必需？ |
|---|---|---|---|
| Python | — | 跑脚本 | **是**（3.8+，无需第三方库） |
| `cfr`（或 Procyon） | `EOS_CFR` / `EOS_PROCYON` / `EOS_TOOLS_DIR` | 反编译源码 | 要读源码时 |
| `javap` / `java` | `EOS_JAVAP` / `EOS_JAVA` / `JAVA_HOME` | 字节码指令文本、交叉验证 | **否** |

```powershell
# 看本机工具解析结果（找不到会显示 null，不影响纯 Python 主流程）
& $PY -X utf8 "$SK\env.py"
```

- **JDK 8 即可，不需要 JDK 17**。`javap`/`java` 只在需要字节码文本或做对照时才用——**行号表与仲裁默认纯 Python，完全不需要 JDK**。
- 反编译器优先 **CFR**（出码质量高、注解完整）；CFR 偶发失败（如 try-with-resources + 合成 finally 导致 `catch` 悬空）时，**用 Procyon 反编译同一个类做交叉修复**。
- 可把工具放进技能自己的 `tools/` 目录，或在 `PATH` 里，或用环境变量指定——三种方式都会被找到。
- 工作目录：`EOS_RC_WORK`（默认 `~/.log-to-rootcause/work`）。**永远不写介质**。

**`javap` 的开销实测**：`-p -l` 约 0.5–0.9 s/次，`-p -l -c` 约 0.6–0.9 s/次。冷盘首次可能异常慢（出现过 14.8 s 离群值），别据此判断卡死。

---

## ① 提取堆栈帧

日志帧的通用格式：

```
at <全限定类名>.<方法>(<源文件名>:<原始行号>) ~[<实际加载的 jar 名>:na]
at com.example.svc.OrderService.submit(OrderService.java:366) ~[order-core-1.4-patch-20250408.jar:na]
   └─ 全限定类名.方法名        └─ 源文件名:原始行号              └─ 运行时真实加载来源
```

要点：

- **`~[...]` 是运行时真实加载来源，是最硬的第一手证据**。`~[x.jar:na]` 表示由该 jar 提供；`[x.jar!/:版本]` 表示来自依赖 jar。有它就以它为准，其余推断只作交叉验证。
- 有些日志格式没有 `~[...]`（如截图里只有 `at com.xx.Class.method(Class.java:224)`）——此时才需要用 ③ 的加载序 + 行号指纹去反推生效版本。
- 同一异常常出现**重复堆栈**（第一次打印 + `Caused by` 链），去重后取**最内层**（最后一个 `Caused by`）为真因。
- 合成方法名要认识：`lambda$<外层>$N`、`$$FastClassBySpringCGLIB$$`、`$$EnhancerBySpringCGLIB$$`、`$1`/`$2` —— **这些在源码里搜不到**，必须映射回外层方法（见 ③.5 R2）。
- 行号可能缺失（`Unknown Source`、`Native Method`）——此时只能靠方法名 + 字节码推断。
- **没有任何一帧属于你的产品代码**时（全是框架帧），真因可能被吞掉了——见 ⑥ 的「光秃秃的 NPE」。

---

## ② 判定类归属

对堆栈里每个**产品自研**类（非 `java.*` / `jakarta.*` / `org.springframework.*` / 已知第三方），
用 `find_class.py` 一次给出**全部副本 + 来源 + 覆盖关系**：

```powershell
& $PY -X utf8 "$SK\find_class.py" <FQCN> --media $MEDIA
```

**顺序原则**：覆盖目录内的副本**先看**（它才是生效版本，见上一节）；未命中再看主归档。
`find_class.py` 一次列全，无需手工分两趟。

### ⚠️ 一个归档里有**多个**放类的地方，都要查

实测踩过的坑：**只扫嵌套 jar 会漏掉全部应用类**。

| 位置 | 存法 | 介质 B | 介质 A |
|---|---|---|---|
| `BOOT-INF/classes/**/*.class` | **裸 class 条目**（不在嵌套 jar 里） | **414 个应用类**（407 个产品类） | 仅 **1** 个 |
| `BOOT-INF/lib/*.jar` | 嵌套 jar | 373 个 | 827 个 |
| war：`WEB-INF/classes/` + `WEB-INF/lib/` | 同上两种 | — | — |

**两种打包风格**：介质 B 把整个应用打进 `BOOT-INF/classes/`；介质 A 几乎全部打进嵌套 jar。
所以**不能假设"产品代码全在嵌套 jar 里"**，也不能只扫 `classes/`。

实测复现（修复前）：某应用类，`--full` 扫了 373 个嵌套 jar、146152 个类，仍报 `NOT FOUND`，**而该类就在归档里**。

> 另一半点：**分层归档顶层**几乎不含业务类（两个介质顶层自有的 loader 类都是 74 个）。
> 业务类要么在 `classes/`，要么在嵌套 jar 里——`find_class.py` 两处都扫。

---

## ③ 覆盖 / 版本仲裁（**不限于任何厂商机制**）

**通用模型**：启动时把某个目录挂在应用归档**之前**（Spring Boot 的 `-Dloader.path=`、
插件目录、`CLASSPATH` 前插、解压式补丁目录……），**同名类由加载顺序靠前者胜出**。

`find_class.py` 的加载序是**可测量事实**推导出来的，不是厂商约定：

1. **覆盖目录里的任何东西 > 主归档**（这是覆盖目录存在的唯一意义）；
2. **同一目录内，按文件名的 ASCII 排序**（这正是 JVM 处理 classpath 目录中 jar 的方式）；
3. **前导零**是厂商/脚本给文件名加的顺序标记，通过 ASCII 排序**自然生效**，不需要专门规则。

```
实测介质 A（19 个 patch jar，全部在同一个 lib/ 目录）：
 13 个零  0000000000000...-20250917-<工单>.jar   ← ASCII 最先，优先级最高
 12 个零  000000000000...-<工单>.jar
  8 个零  00000000...-20250408.jar               ← 本例实际生效者
  6 个零  000000...-2025-02-27-all.jar
  0 个零  <产品>-7.2.0.jar（主归档内原版，被全部 patch 覆盖）
```

**必须避开的两个错误推断**：

1. **不要按日期推断优先级**。实测反例：`...20250516`（10 个零）优先级**高于**`...20250529`（9 个零）——日期晚反而排在后面。**只看排序结果。**
2. **不要假设"新补丁排在旧补丁前"**。前导零是人工/脚本加的顺序标记，与日期、版本号无必然关系。

> 若日志带 `~[...]`，**以它为准**并与上述推断交叉验证；两者不一致时说明加载路径被改过（换过 loader.path、替换过 jar）。

### 用 CRC 先去重

多个候选常常**装着完全相同的类体**。用 `ZipInfo.CRC` 分组（零成本），只对**每个不同类体**解析一次：

```
实测：4 个候选 jar → 只有 2 个不同类体
  7e301af9  x1  00000000...-20250408.jar
  c9aa7774  x3  000000...-2025-02-27-all.jar | 0000...-2025-01-10-all.jar | 000...-2024-12-17-831.jar
```

`find_class.py` 已内置去重并输出该表。

### 行号指纹（最硬的判别法）

若多个候选的同一个方法**源码行号表不同**，日志行号本身就能**唯一指认**生效的那一份。实测（日志行 366）：

| 候选 | 前导零 | 该方法 LineNumberTable | 含日志行 366 |
|---|---|---|---|
| 主归档内原版 | 0 | 325–332 | ✗ |
| `00000000...-20250408.jar` | 8 | **363–370** | **✓ 唯一** |
| `000000...-2025-02-27-all.jar` | 6 | 383–390 | ✗ |
| `0000...-2025-01-10-all.jar` | 4 | 383–390 | ✗ |
| `000...-2024-12-17-831.jar` | 3 | 383–390 | ✗ |

行号指纹与 ASCII 排序**各自独立**地指向同一个 jar，构成双重确证。

仲裁结果要与堆栈里 `~[jar名]` 的实测值**交叉验证**：两者一致才可信；不一致说明生效者另有其人（如 `loader.path` 被改、或 jar 被替换）。

---

## ③.5 行号兼容性（**日志行号对不上时的三条出路**）

日志里的行号经常**在目标 class 里找不到对应行**。这不是异常，而是三种机制造成的，必须分别处理。**判据不能只看"行号在不在方法区间内"。**

### 为什么不能用「区间判断」

`javac -g` 只给**产生了字节码的行**记录行号。注释、空行、跨行的声明都**没有行号行**。所以行号表是**稀疏集合**，不是连续区间。

实测 `MDMAfcService`（20250408 版）：**60 个成员中含空洞的占 34 个（57%）**；1111 行区间里 **673 行（61%）无字节码行**。（另一候选版 2025-02-27：62 个成员、1039 行区间、629 行空洞，同样是 61%。）

`findEmp` 的行号表就是 `[363, 364, 365, 366, 370]` —— **367、368、369 是空洞**。于是：

```
❌ 错误判据： min(table) <= 日志行 <= max(table)
   line 367 → rangeHit=True 但 exactHit=False   <-- 假阳性
```

**正确判据：建全类 `行号 → 归属方法` 映射，做精确匹配。**

### 三种情形与对策

| 情形 | 症状 | 判据 | 对策 |
|---|---|---|---|
| **R1 空洞行** | 日志行落在某方法区间内，但该行号表里没有这一行 | 上下最近的两个行号**属于同一个方法** | 该行是方法内的注释/空行/声明行；读该方法内这两行之间的语句 |
| **R2 合成方法** | 该行在外层方法的"空洞"里，其实属于它的 lambda | 该行号归属于 `lambda$<外层>$N` | 通过命名约定还原外层方法（见下） |
| **R3 跨版本漂移** | 该行号在**本版本**属于**另一个方法**，或完全越界 | 上下最近行号**属于不同方法** / 行号越界 | 用 `--cross` 按方法锚点**换算**行号 |

**R1 与 R2 其实是同一现象的两面**——实测 `findEmp` 的空洞正好由它的 lambda 填满：

```
line 363 → findEmp             (bytecode offset 0)
line 364 → findEmp             (offset 4)
line 365 → findEmp             (offset 12)
line 366 → findEmp             (offset 22)
line 367 → lambda$findEmp$10   (offset 0)   ← 看起来是 findEmp 的"空洞"
line 368 → lambda$findEmp$10   (offset 14)
line 369 → 无字节码（注释/空行）
line 370 → findEmp             (offset 35)
```

所以要建**整类**映射（含合成方法），而不是逐个方法判断。

### R3：跨版本行号的换算（**没有全局偏移量**）

patch 在某处插入代码后，**其后所有行号平移**。但**平移量因方法而异**。实测同一类的两个 patch 之间，54 个共有成员的偏移呈 **8 种不同值**（含**负偏移**，即方法整体前移）：

```
delta (B−A): -13 x1, +17 x3, +20 x25, +23 x5, +35 x1, +36 x2, +39 x1, +40 x16
```

> ⚠️ **任何单一偏移量都不能完成跨版本换算。** 既有 +17 也有 +40，甚至还有 **−13**——必须**逐方法**锚定。

**唯一可靠的锚点是「方法名 + 方法内字节码偏移」**——因为不管别处插了多少代码，方法内偏移都稳定：

```
实测换算（日志来自 20250408 版，line 366 → findEmp，字节码偏移 22）
  → 2025-02-27 版：findEmp 区间 383-390，偏移 22 对应 LINE 386
    换算结果：366 → 386  (delta +20)
```

**换算不成立时的兜底**：若某版本里**方法名根本不存在**（被 patch 改名/删除），说明该版本不可能是日志来源，直接排除该候选。

### 解析 javap 输出：**不要用正则去切方法块**

`javap -p -l` 的输出必须**逐行**解析，不能靠正则匹配方法声明。用「`修饰符 类型 方法名(`」这种正则会**静默丢成员**：

| 丢什么 | 为什么正则匹配不到 |
|---|---|
| **构造函数** | `public com.x.Y();` 类型路径与方法名之间**没有空格**，`...+\s+名字\(` 匹配失败，构造函数的行号被并进上一个成员 |
| **`static {}` / `<clinit>`** | **完全没有括号**，任何以 `(` 结尾的正则都匹配不到 |

**实测规模**（某介质抽样）：60 个类的审计出现 **62 处违规**；该介质 142 个产品类里 **72 个含 `static {}`**、**120 个含构造函数**——几乎每个类都会被带偏。

**正确判据**（javap 输出里唯一成立的规律）：
> 成员声明行 = **恰好两个前导空格** 且 **以 `;` 结尾**。

字段、构造函数、`static {};`、普通方法都满足；表行缩进更深、区块头以 `:` 结尾。逐行扫，遇到声明行就开新成员。

**已封装成 `scripts/javap_parse.py`**，定位脚本都调用它，不要各自再写解析。校验不变量：`解析出的成员数 == javap 输出里 "LineNumberTable:" 的出现次数`。

另需注意：堆栈里构造函数写作 **`<init>`**，而 javap 打印为**类简名**（嵌套类保留 `$`）——按名查找时两种写法都要接受。

### 命令

```powershell
$PY    = 'python'      # 或本机 python 的绝对路径
$SK    = "$env:USERPROFILE\.dsh\skills\eos-log-to-rootcause\scripts"
$MEDIA = '<产品安装目录>'

# 布局总览（先跑这个）
& $PY -X utf8 "$SK\find_class.py" --media $MEDIA --layout

# 定位 + 列出全部副本 + 用日志行号仲裁生效版本（自动识别空洞/合成/跨版本）
& $PY -X utf8 "$SK\find_class.py" <FQCN> --media $MEDIA --method <方法> --line <行号>

# 行号归属消歧 / 跨版本换算
& $PY -X utf8 "$SK\line_lookup.py" <FQCN> --lib <覆盖目录> --method <方法> --line <行号>
& $PY -X utf8 "$SK\line_lookup.py" <FQCN> --lib <覆盖目录> --method <方法> --line <行号> --cross
```

---

## ④ 反编译

**务必带上完整 classpath**，否则泛型和依赖类型全部退化成 `Object`，可编译性与可读性都暴跌（实测：不带 classpath 时 171 个文件里 158 个含未解析类型；带上后降到 108 个）。

```powershell
# 覆盖目录 + 主归档 里的 jar 全部作为 extraclasspath
$OVR = (Get-ChildItem '<覆盖目录>' -Filter *.jar -Recurse | ForEach-Object FullName)
$ECP = ($OVR -join ';')
java -jar <cfr.jar> <③仲裁胜出的jar> --outputdir <输出目录> --caseinsensitivefs true --extraclasspath "$ECP"

  <③仲裁胜出的jar> --outputdir <输出目录> --caseinsensitivefs true --extraclasspath "$ECP"
```

- `--extraclasspath` 只接受**一个**参数，jar 之间用 `;` 分隔；**不要**把多个 jar 作为多个参数传入（会被当成多个输入 jar，产出暴增）。
- ⚠️ **CFR 不接受"目录"作为输入**：传一个目录进去会 **rc=0、无任何输出、无报错**，静默产出 0 个文件（实测）。要用 **jar** 或**单个 `.class` 文件**作输入；类散落在目录里时，逐个 `.class` 传，或先用 `-cp <目录>` + `--extraclasspath` 组合。
  - 实测对照：目录输入 → rc=0、0 文件；单 `.class` 输入 → rc=0、1 文件；jar 输入 → rc=0、52 文件。
- 若目标类是"应用类"（存在集群归档的 `classes/` 里而非嵌套 jar），用 `find_class.py --json` 看 `kind=app-classes`，
  再把它按包路径解出来成 `.class` 文件喂 CFR（见下条）。
- 若目标类**不在覆盖目录**（未被 patch），就先解出提供它的那份（jar 或 `classes/` 下的 class）再反编译。
- 中文注解会被输出成 `\u9879\u76ee` 形式——**内容没丢**，做一次 unicode 反转义即可，不要误判为乱码损坏。
- 只反编译**堆栈涉及的那几个类**通常就够定位；全量反编译留给需要通读逻辑的场景。
- 需要反编译**大批类**且 classpath 很长时，命令行会超 Windows 限制：实测一个 1284 jar 的 classpath 达 **98,961 字符**（上限 32,767）。改用 `@argfile` 传参，或先只挑相关的产品 jar。

---

## ⑤ 行号对齐（**本技能的关键步骤**）

**问题**：`javac -g` 保留的原始行号与反编译输出的行号**不是一回事**。反编译器会重排 import、合并声明、展开语法糖，行号整体漂移。

实测两个真实案例（偏移量按**同一语句**在两个编号体系中的差值计算）：

| 类 | 日志行号 | 该行语句在反编译产物中的行 | 偏移 | 反编译文件总行数 |
|---|---|---|---|---|
| 案例一 | 366 | 344 | **+22** | 723 |
| 案例二 | 166 | 95 | **+71** | 130 |

注意第二例：日志说 166 行，但反编译产物**只有 130 行**——照日志行号去数，永远数不到。**偏移量因类而异，不可套用。**

> ⚠️ 别把「文件总行数」当成对齐基准。日志行 366 若拿总行数 723 去减，会得到 −357 这种荒谬值。**偏移量只在具体语句之间有意义。**

**正解：读 class 文件里的 `LineNumberTable`（权威行号映射）。**

```powershell
# 纯 Python（默认，不需要 JDK）——推荐
& $PY -X utf8 "$SK\line_lookup.py" <FQCN> --lib <覆盖目录> --method <方法> --line <行号>

# 需要字节码指令文本时，才用 javap（路径由 env.py 自动定位）
& $PY -X utf8 -c "import sys;sys.path.insert(0,r'$SK');import env,subprocess;print(subprocess.run([env.javap_path(),'-p','-l','-c','-cp',r'<目标jar>',r'<FQCN>'],capture_output=True,text=True).stdout)"
```

输出中每个方法的 `LineNumberTable` 给出 `line <原始行号>: <字节码偏移>`。用它把日志行号锚定到具体字节码指令，再看该指令调用/操作了什么。

> 找不到对应行时先回看 **③.5 行号兼容性**：区分「空洞行 / 合成方法 / 跨版本漂移」三种情形，别盲目换 jar。定位方法用 `line_lookup.py`，它会把日志行号解析到归属方法，并在需要时跨版本换算出正确行号。

**实测（日志行 366，胜出 jar CRC `7e301af9`）**：

```
LineNumberTable:
  line 363: 0      ← 方法体第一条语句
  line 364: 4      ← ArrayList<Employee> employeeList = new ArrayList<>();
  line 365: 12     ← IEmployeeAPI empAPI = this.afcClient.getEmployeeAPI();
  line 366: 22     ← userIds.forEach(e -> employeeList.add(empAPI.queryEmployeeByUserId(e)));
  line 370: 35     ← return employeeList;

--- 偏移 22 处的字节码 ---
  22: aload_1
  23: aload_2
  24: aload_3
  25: invokedynamic #92,  0   // accept:(List;IEmployeeAPI;)Consumer
  30: invokeinterface #16,  2  // List.forEach:(Ljava/util/function/Consumer;)V
```

日志行 **366** 锚定到偏移 **22**，对应 `forEach` 那一行；而**反编译产物里这一行是第 344 行**——这就是偏移 +22 的来源（366 − 344 = 22；同时 363 − 341 = 22 也成立，因为方法起始行同时平移）。

一行内出现 `getEmployeeId()` 紧跟 `invokeinterface`，即可判定该语句是**在一个方法返回值上继续调用**——若前一步返回 `null`，此处必 NPE。

**合成方法的处理**：`lambda$findEmp$10` 这类合成方法，其 `LineNumberTable` 用的是**外层方法源码里的行号区间**（实测 `lambda$findEmp$10` = 367–368 行，正好是 `findEmp` 中第 366 行那个 `forEach` 的 lambda 体所跨的源码行；对照另一候选 jar 则是 387–388，与其 `findEmp` 的 383–390 同区）。所以：

- 在源码里搜 `lambda$findEmp$10` → **搜不到**（它是编译器生成的）；
- 正确做法：取它的行号 → 回到**外层方法**（`findEmp`）→ 找落在同一行区间的 lambda/`forEach` 调用；
- 注意合成方法的行号**不与外层方法首行重合**（367–368 ≠ 366），所以**不要**拿它去套外层方法的行号指纹；用外层方法的行号区间去归属即可。

**低配替代法**（当 class 缺 `-g` 无行号表时）：先反编译，找到目标方法，用**方法内相对偏移**对齐——日志行号 − 方法起始行号，再用同样的差值数反编译产物里该方法内的语句。不如 `LineNumberTable` 精确，但比裸数行号可靠。

---

## ⑥ 根因定性

定位到语句后，必须**区分问题性质**，因为处置方式完全不同：

| 性质 | 特征 | 处置 |
|---|---|---|
| **代码缺陷** | 空值未判、边界未处理、并发竞争；语句逻辑本身有问题 | 出补丁：修判断/加保护/改调用方 |
| **环境/配置** | License 过期、Redis 版本过低、连接串错、JDBC 驱动不匹配 | 改配置或换依赖，**改动代码无用** |
| **版本/补丁错配** | patch 覆盖了旧实现、patch 之间冲突、补丁未生效 | 调整前导零顺序或撤下冲突 patch |
| **数据问题** | 传入数据不存在/越权/跨租户；代码对数据做假设 | 查数据源，代码侧建议加健壮性 |

**别把环境问题当代码 bug 改**。实测案例：`java.lang.Error: License Error: EE.expiration:license authorized 2024-06-05;current date:Thu Oct 30 00:00:00 CST 2025` 导致 AppServer 子容器启动失败、应用起不来——这需要重新签发 License，反编译到这里就没有下文了。

### ⚠️ 「光秃秃的 NPE」：真因往往被**代理吞掉了**

实测案例（executor 介质）：日志只有一句
`java.lang.NullPointerException`，栈顶是 `RowMapperResultSetExtractor.extractData:93`，**没有 `Caused by`，也没有更深的业务帧**。看着像框架问题，实际是产品自己的连接池把异常吞了。

**判据：如果 NPE 的栈顶帧直接落在框架内部、且 `rs.next()` 这类调用没有更深的驱动帧，说明"接收者为 null"**——JVM 在 `invokeinterface` 处就抛了，不会产生更深帧。

```java
// 崩溃点（字节码）：接收者 rs 为 null，故 .next() 直接 NPE，无更深帧
93: aload_1
94: invokeinterface ResultSet.next()Z

// rs 的来源：JdbcTemplate$1QueryStatementCallback
line 452: stmt.executeQuery(sql) -> astore_2   // rs = 返回值
line 453: rse.extractData(rs)                  // 未判空，直接传下去

// 真凶：产品自研 DataSource 的动态代理把异常吞掉并 return null
catch (Exception e2) {
    if (Boolean.parseBoolean(System.getProperty("dqms.executor.debug"))) {
        e2.printStackTrace(pw);     // ← 只有开这个开关才记录真异常
    }
    return null;                    // ← 异常被丢弃，返回 null
}
```

**遇到"光秃秃的 NPE"时的排查顺序**：

1. **看栈顶方法内字节码**：NPE 落在哪种指令上。落在 `invokeinterface`/`invokevirtual` 且**无更深帧** → 接收者为 null；落在 `getfield` → 某对象的字段访问前该对象为 null。
2. **顺着数据来源往上追**：那个 null 是从哪个方法返回的（本例是 `executeQuery`）。
3. **找"吞异常"的代理/包装类**：产品自研的 `DataSource`/`Connection`/`Statement` 包装、动态 `InvocationHandler`、`catch (Exception e) { return null; }` 是典型元凶。**它们会让真异常完全不进堆栈**。
4. **找调试开关**：这类代码常留 `-Dxxx.debug=true` 才打印被吞异常——**先开它复现**，比反编译猜快得多。
5. 定性：这属于**代码缺陷**（异常处理吞掉真因 + 调用方未判空），不是环境问题。

> 教训：**"栈里没有真因"本身就是证据**。不要因为栈短就下"框架 bug"的结论——先问"真正的异常去哪了"。

---

## 换一个新产品时：适配清单

本技能**不绑定产品**，换产品时按此顺序做，通常 5 分钟内可用：

| 步骤 | 做什么 | 命令 / 判据 |
|---|---|---|
| 1 | **看布局** | `find_class.py --media <目录> --layout` —— 确认主归档、覆盖目录、命名空间被正确识别 |
| 2 | **确认工具** | `env.py` —— 反编译器能找到就够；JDK 可选 |
| 3 | **取一个已知类试跑** | 从日志里挑一个非框架类：`find_class.py <FQCN> --media <目录>` |
| 4 | **核对覆盖目录** | 若 `overrideDirs` 为空但你确信有补丁目录，用 `EOS_OVERRIDE_DIRS` 显式指定（见下） |
| 5 | **核对命名空间** | 若 `appNamespaces` 与产品包名不符，纯属展示问题，不影响定位 |
| 6 | **跑测试** | 换介质后建议 `tests/run_tests.py`（用 `EOS_LIB` 指向新介质的覆盖目录） |

**什么时候需要人工指路**（自动发现覆盖不到的情况）：

- 覆盖目录名不带任何常见线索（如叫 `xyz/`），且其中 jar 与主归档**没有同名类交集**（纯新增类）；
- 类从安装目录**之外**加载（共享 lib 目录、容器镜像层、远端 classloader）；
- 部署脚本用**运行期参数**指定加载路径（此时去读 `bin/` 下的启动脚本，`media.py` 会把它们列出来）。

对应手段（都可选，不影响默认流程）：

```powershell
# 显式告诉技能哪些目录是覆盖区/主归档
$env:EOS_OVERRIDE_DIRS = 'D:\path\to\patch;D:\path\to\hotfix'
$env:EOS_MAIN_ARCHIVE = 'D:\path\to\app-exec.jar'
```

> 设计原则：**能自动发现的绝不写死，发现不了的留显式开关**。
> 这两条环境变量就是"发现不了"时的唯一出口，其余场景一律自动。

---

## 判定规则速查

| 现象 | 判定 |
|---|---|
| 堆栈类在覆盖目录里能找到 | 该类被覆盖过，**那份是完整类体**，直接分析 |
| 堆栈类只在主归档里能找到 | 未被覆盖，反编译主归档里那份 |
| 日志带 `~[xxx-patch-yyy.jar]` | 该 jar 生效，覆盖了主归档原版；**以此为准** |
| 多个位置都有同一个类 | 先按 CRC 去重，再按 **ASCII 加载序 + 覆盖目录优先** 仲裁，**不看日期** |
| 多份的同方法行号表不同 | **行号指纹**：日志行号落在哪个表内，就是哪一份 |
| **多份都含该行号** | 各版本自身合法 → 由**加载顺序**决定（覆盖目录优先，同目录按 ASCII）；提示用户用 `~[...]` 交叉验证 |
| **日志行号在方法区间内但没有该行** | **空洞行（R1）**：注释/空行/声明行无字节码行；看上下邻居是否同属一个方法 |
| **日志行号落在某方法的"空洞"里** | **可能是合成方法（R2）**：该行其实属于 `lambda$<外层>$N`，用命名约定还原外层方法 |
| **同一行号在两份里属于不同方法** | **跨版本漂移（R3）**：覆盖时插入了代码；用 `--cross` 按「方法+字节码偏移」换算 |
| **某版本里方法名不存在** | 该版本不可能是日志来源，直接排除 |
| **定位报 NOT FOUND，但包名看着就在介质里** | 应用类可能在集群归档的 `classes/`（裸 class，不在嵌套 jar）；确认工具已扫两处 |
| **CFR 传目录进去、rc=0 却没有产物** | CFR **不支持目录输入**，静默产出 0 文件；改用 jar 或单个 `.class` |
| **NPE 栈很短、无 `Caused by`、栈顶就在框架里** | 真因可能被**代理吞掉**：查自研 DataSource/Connection 包装里的 `catch { return null; }` 与 `-Dxxx.debug` 开关 |
| 日志行号 > 反编译文件总行数 | 正常，反编译产物行号缩水；用行号表对齐 |
| 方法名含 `lambda$` / `$$FastClass` / `$$Enhancer` | 合成方法，搜源码必失败；映射回外层方法 |
| `Caused by` 链最内层是 `java.lang.Error` | 多为框架级致命错误（License/初始化），非业务 bug |
| 反编译出 `Could not load the following classes` | classpath 不全；补 `--extraclasspath` 重编 |
| CFR 产出语法错误（如 `catch` 悬空） | 用 Procyon 反编译**同一个类**替换该文件 |

---

## 已证实的坑

1. **别先啃主归档**。被覆盖过的核心业务类在覆盖目录里就是**完整类体**，直接分析即可。全量索引主归档要多花 15 倍时间、多读 379 MB，且会漏掉覆盖版本新引入的类。
2. **PowerShell 5.1 的 `>` 重定向写 UTF-16**，生成的报告 `read` 会判为 binary。报告一律**由 Python 以 `encoding='utf-8'` 直接写文件**，不要靠 shell 重定向。
3. **PowerShell 把 java 的 stderr 当错误**：`--version` 正常输出却报 `NativeCommandError` + `exit code 1`。判成功要看**输出内容**，不要只看退出码。
4. **中文日志/源码的读取**：控制台按 GBK 解码会把 UTF-8 中文显示成乱码。取证走 `read` 工具或 `python -X utf8`，结论落 UTF-8 文件，**不要用控制台乱码判定内容损坏**。
5. **`javap` 不加 `-p` 看不到 private 合成方法**；查 `lambda$` 必须加 `-p`。
6. **Windows 路径分隔符**：classpath 用 `;`，Linux/macOS 用 `:`；在 PowerShell 里传含空格的路径要加引号。
7. **Python 的 `str.rstrip('.class')` 是按字符集剥离**，会把 `...EmployeeLocalImpl` 末尾的 `l` 一起吃掉（`i`/`m`/`p` 也在集合里）。剥后缀一律用 `endswith` + 切片。
8. **索引键格式必须统一**：zip 里的类是 `a/b/C.class`，而查询用的是 `a.b.C`。建索引时就去掉 `.class` 并转成点号，否则永远查不到。
9. **绝不能用 `min(table) <= 行号 <= max(table)` 判断行号命中**。行号表是**稀疏**的（实测 61% 的区间行没有字节码行），区间判断会对空洞行产生**假阳性**，进而错误地"仲裁"出 jar。必须做**精确行号匹配**。
10. **不要试图用一个全局偏移量换算跨版本行号**。实测同一类两个版本之间，各方法偏移分布在 **−13 ～ +40 共 8 种值**上（含负偏移）。锚点只能是「**方法名 + 方法内字节码偏移**」。
11. **行号相同的两行，可能属于不同方法**（实测同一行号在两个版本里分属不同方法）。因此**行号必须与「预期方法名」联合判据**，`--method` 参数就是为此存在。
12. **绝不要用正则切 javap 的方法块**。构造函数（类型与名字间无空格）和 `static {}`（无括号）会被**静默丢弃**——实测 60 个类里 62 处违规。唯一可靠判据：**恰两空格缩进 + 以 `;` 结尾** 即成员声明行。用 `javap_parse.py`。
13. **堆栈里的 `<init>` 与 javap 的类简名是同一个东西**。按方法名查找时两种写法都要接受，否则构造函数帧永远匹配不上。
14. **不要用 javap 做批量/循环解析**。每次调用起一个 JVM，实测 0.36 s/次；解析全部 413 个产品类要 ~219 s，而纯 Python 读 class 文件只要 **0.44 s（178 倍）**。行号表就在 class 文件里，用 `classfile.py`；仅在需要**字节码指令文本**或做对照时用 javap。
15. **不要假设"产品代码全在嵌套 jar 里"，也不要假设全在 `classes/` 里**。实测两套介质恰好相反：一套 414 个应用类全在 `BOOT-INF/classes/` 裸条目，另一套 827 个嵌套 jar 里而 `classes/` 只有 1 个类。**两个位置都要查**（`find_class.py` 已覆盖）。
16. **CFR 不能吃目录**。目录输入会 rc=0、静默产出 0 文件、无任何报错，极易误判为"反编译失败但原因不明"。用 jar 或单个 `.class` 文件。
17. **不要用"文件总行数"或"目测某行"来对齐日志行号**。必须逐行走 `LineNumberTable` 把每个源码行映射到字节码偏移（`line 224 -> offset 1428`），再读该偏移的指令。本项目就曾因把 `extractData:93` 误当成 `mapRow` 那行（实际在 94 行）而**先给出了错误结论**——行号对齐不是可选项。
18. **"栈短、无 Caused by" 不等于"框架 bug"**。这往往是自研代理/包装类 `catch (Exception) { return null; }` 吞掉了真异常。先找调试开关（如 `-Dxxx.debug=true`）复现拿到真栈，再定性。
19. **不要按类数挑"主归档"**。依赖 jar 的顶层类数常远超应用归档（实测某依赖 jar 1253 个类，而同介质主归档自带类只有 1 个）。判据必须是**打包形态**（分层/`classes/`/嵌套 jar）。
20. **覆盖目录里的 jar 一律优先于主归档**。若仲裁只看文件名排序而忽略"是否在覆盖目录"，主归档会赢下同分比较，从而**把未打补丁的旧版判成生效版本**（实测踩过：两个版本都含日志行号时判反了）。

---

## 输出格式

分析结论按此结构给出，每一层都带证据来源：

1. **故障定性** —— 一句话，并标明「代码缺陷 / 环境配置 / 版本错配 / 数据问题」。
2. **证据链** —— 从异常入口到崩溃点的逐帧表：类、方法、行号、**提供该类的来源**（覆盖目录 / 主归档 / 嵌套 jar / `classes/`）。
3. **实际生效版本** —— 若存在多候选，说明仲裁结果（CRC 去重 + 行号指纹 + 加载序）与 `~[jar名]` 是否一致。
4. **根因语句** —— 用 `LineNumberTable` 对齐后的具体语句 + 其字节码依据。
5. **触发条件** —— 什么输入/环境会走到这里。
6. **处置建议** —— 按性质给出：出补丁 / 改配置 / 调加载顺序 / 查数据。

结论中**必须区分「已证实」与「推断」**：前者有堆栈或字节码证据，后者只能在数据不足时作为假设提出。

---

## 铁律

**只读分析。** 不修改产品介质下的任何文件；不启动产品；反编译产物与报告只写入工作区（`EOS_RC_WORK`，默认 `~/.log-to-rootcause/work`）。
日志里可能含密码、Token、内网地址——报告中**一律掩码**，只保留键名与结构。
